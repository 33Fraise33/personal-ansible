- name: DATE - Set timezone to UTC
  timezone:
    name: "Etc/UTC"

- name: DATE - Install ntp package
  apt:
    name: ntp

- name: DATE - Enable & start ntp
  systemd:
    name: ntp
    state: started
    enabled: yes